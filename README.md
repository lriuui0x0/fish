# NOTE

1. Add private ppa and install fish. `sudo apt-add-repository ppa:fish-shell/release-3`.
2. This repository includes some fzf setup. Clone `https://github.com/junegunn/fzf.git` to `/usr/local/bin/fzf`, and run `fzf/install`.

